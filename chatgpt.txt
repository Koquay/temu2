The following function displays a specified modal:

showdModal = (modalId: string) => {
    console.log('showdModal() called')
    const modalElement = document.getElementById(modalId);
    if (modalElement) {
      const modal = new bootstrap.Modal(modalElement);
      modal.show();
    }
  }

I am dedaling with numerous modals. I need a function that tells me the current modal that is being displayed.